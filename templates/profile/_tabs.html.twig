<nav class="rounded-2xl border border-[#dbe6ff] bg-white p-2 shadow-sm" aria-label="Разделы кабинета">
    <div class="flex flex-row gap-2 overflow-x-auto lg:flex-col lg:overflow-visible">
        <a href="{{ path('profile') }}"
           class="shrink-0 rounded-xl px-4 py-3 text-sm font-semibold transition lg:w-full
           {{ app.request.attributes.get('_route') == 'profile'
              ? 'bg-[#0b5fff] text-white shadow-sm'
              : 'text-slate-700 hover:bg-[#eef4ff] hover:text-[#0b5fff]' }}">
            Профиль
        </a>
        <a href="{{ path('profile.orders') }}"
           class="shrink-0 rounded-xl px-4 py-3 text-sm font-semibold transition lg:w-full
           {{ app.request.attributes.get('_route') == 'profile.orders'
              ? 'bg-[#0b5fff] text-white shadow-sm'
              : 'text-slate-700 hover:bg-[#eef4ff] hover:text-[#0b5fff]' }}">
            Активность и заказы
        </a>
    </div>
</nav>
